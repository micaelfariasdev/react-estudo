# https://www.nginx.com/blog/using-free-ssltls-certificates-from-lets-encrypt-with-nginx/
#
# REPLACES
# micaelfarias.com = Replace with your domain
# /home/ubuntu/react-estudo = Replace with the path to the folder for the project
# 
# For letsencrypt and Ubuntu:
# sudo apt update -y && sudo apt upgrade -y && sudo apt autoremove -y
# sudo apt install nginx certbot python3-certbot-nginx -y
# sudo openssl dhparam -out /etc/ssl/certs/dhparam.pem 4096
# sudo systemctl stop nginx
# sudo certbot certonly --standalone -d micaelfarias.com
# sudo chmod -R 755 /home/ubuntu/react-estudo
# sudo nano /etc/nginx/sites-available/micaelfarias.com
# Add contents of this file
# sudo ln -s /etc/nginx/sites-available/micaelfarias.com /etc/nginx/sites-enabled/micaelfarias.com
#
# Set timezone
# List - timedatectl list-timezones
# sudo timedatectl set-timezone America/Sao_Paulo
#
# HTTP
server {
  listen 80;
  listen [::]:80;
  server_name micaelfarias.com;
  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl http2;
  listen [::]:443 ssl http2;

  server_name micaelfarias.com;

  ssl_certificate /etc/letsencrypt/live/micaelfarias.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/micaelfarias.com/privkey.pem;
  ssl_trusted_certificate /etc/letsencrypt/live/micaelfarias.com/chain.pem;
  ssl_session_cache shared:SSL:10m;
  ssl_session_timeout 5m;
  ssl_prefer_server_ciphers on;
  ssl_ciphers ECDH+AESGCM:ECDH+AES256:ECDH+AES128:DH+3DES:!ADH:!AECDH:!MD5;
  ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
  ssl_dhparam /etc/ssl/certs/dhparam.pem;

  add_header Strict-Transport-Security "max-age=63072000; includeSubdomains";
  ssl_stapling on;
  ssl_stapling_verify on;
  resolver 8.8.8.8 8.8.4.4 valid=300s;
  resolver_timeout 5s;

  # root da aplicação React (dist gerado pelo Vite)
  root /home/ubuntu/react-estudo/dist;
  index index.html;

  # Redirecionamento para index.html (SPA)
  location / {
    try_files $uri /index.html;
  }

  gzip on;
  gzip_disable "msie6";
  gzip_comp_level 6;
  gzip_min_length 1100;
  gzip_buffers 4 32k;
  gzip_proxied any;
  gzip_types
    text/plain
    text/css
    text/js
    text/xml
    text/javascript
    application/javascript
    application/x-javascript
    application/json
    application/xml
    application/rss+xml
    image/svg+xml;

  access_log off;
  error_log /var/log/nginx/micaelfarias.com-error.log;
}
